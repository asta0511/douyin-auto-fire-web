import './globals.css'

export const metadata = {
  title: '抖音续火花 - 配置管理',
  description: '管理 douyin-auto-fire 的 GitHub Secrets 配置',
}

export default function RootLayout({ children }) {
  return (
    <html lang="zh-CN">
      <body>{children}</body>
    </html>
  )
}